server:
  port: ":8080"
  shutdown_timeout_seconds: 10

database:
  dsn: "postgres://basepro:basepro@127.0.0.1:5432/basepro_dev?sslmode=disable"
  max_open_conns: 10
  max_idle_conns: 5
  auto_migrate: false
